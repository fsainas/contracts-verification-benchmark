{
    "name": "Deposit (ERC20)",
    "specification": "file:spec.md",
    "properties": {
        "no-deposit-twice": "`deposit` can only be called once.",
        "wd-contract-bal": "if token is ERC20-compliant, the contract token balance is decreased by `amount` after a successful `withdraw(amount)`.",
        "wd-leq-init-bal": "if token is ERC20-compliant, the overall withdrawn amount does not exceed the initial deposit."
     }
}
